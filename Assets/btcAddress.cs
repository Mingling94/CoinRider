using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class btcAddress : MonoBehaviour {
	public WalletCreate address;
	public Text btcAdd;
	public string strAddress;

	// Use this for initialization
	void Start () {
	//	address = GetComponent<WalletCreate> ();
	//	Debug.Log (strAddress);
	}
	
	// Update is called once per frame
	void Update () {

	}
}
